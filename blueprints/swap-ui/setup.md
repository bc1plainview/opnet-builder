# Swap UI Blueprint — Setup

Scaffold a React frontend that connects to MotoSwap for OP20-to-OP20 token swaps.

## What You Get
- Vite + React + TypeScript frontend
- Token selector with balance display
- Swap interface with slippage settings
- Price impact calculation
- Transaction confirmation flow
- OP_WALLET integration via @btc-vision/opwallet
- Responsive dark theme

## Setup
1. Create a new directory for your project
2. Ask the AI: "Scaffold a swap UI using the swap-ui blueprint"
3. The AI will read the required docs and generate the full project

## Required Docs (AI reads these automatically)
- All frontend docs (see SKILL.md frontend section)
- MotoSwap Router interaction docs
- Wallet connection docs
